SAJAL SINGHAL
sajal.singhal

java pdollar
linux platform

Steps:

Execute pdollar by executing "make clean" on the cmd first.
Run the Makefile using the command "make"
Then run pdollar using the arguments provided.
java pdollar -t <gesturefilename> --> will add the template
	java pdollar -r --> to remove the saved templates
	java pdollar --> will return a help screen	
	java pdollar <eventfilename> --> to recognize the template

